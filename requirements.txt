# ── Core ──────────────────────────────────────────────────────────
pyyaml>=6.0

# ── Vision (NCNN + OpenCV headless — nhẹ nhất cho Pi) ────────────
opencv-python-headless>=4.9
ncnn
numpy>=1.24

# ── Serial (RPi ↔ Arduino) ────────────────────────────────────────
pyserial>=3.5

# ── Web (Flask + SocketIO) ────────────────────────────────────────
flask>=3.0
flask-socketio>=5.3
simple-websocket>=1.0

# ── Training only (cài riêng trên laptop) ─────────────────────────
# ultralytics>=8.0
# torch>=2.0
# Không cần cài trên Pi — chỉ cần NCNN model